using UnityEngine;
using UnityEngine.UI;

public class HealthBarItemView : MonoBehaviour
{
    public Image Fill;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
